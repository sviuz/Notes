namespace Other {
  public class Enums {
    public enum NavigationPanelPosition {
      Left = 1080,
      Middle = 0,
      Right = -1080
    }
    
    public enum NavigationType {
      Left,
      Right,
      Middle
    }

    public enum TimeType {
      Hours = 24,
      Minutes = 60
    }
    
    public enum PanelWindow {
      AddNote,
      OpenNote,
      EditNote
    }
  }
}